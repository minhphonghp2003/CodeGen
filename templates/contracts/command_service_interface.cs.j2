using MyERP.Domain.ViewModels;
using MyErp6.Application.Contracts.{{ feature_name_plural }}.Dtos;
using MyErp6.Domain.Shared.TPosResult;
using TMT.MyErp6.Domain.Shared.Models;

namespace MyErp6.Application.Contracts.{{ feature_name_plural }}
{
    /// <summary>
    /// Định nghĩa các thao tác ghi cho {{ feature_name_plural }}.
    /// </summary>
    public interface I{{ feature_name }}CommandService : IMyErp6ApplicationWriteService<{{ entity_name }}, long>
    {
        /// <summary>
        /// Tạo mới {{ feature_lower }}.
        /// </summary>
        Task<TPosResult<{{ model_name }}>> CreateAsync({{ upsert_dto_name }} model, CancellationToken cancellationToken);

        /// <summary>
        /// Cập nhật {{ feature_lower }} hiện có.
        /// </summary>
        Task<TPosResult> UpdateAsync(long id, {{ upsert_dto_name }} model, CancellationToken cancellationToken);

        /// <summary>
        /// Xóa {{ feature_lower }} theo mã định danh.
        /// </summary>
        Task<TPosResult> DeleteByIdAsync(long id, CancellationToken cancellationToken);
    }
}
