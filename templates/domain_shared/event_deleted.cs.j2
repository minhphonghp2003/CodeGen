using MyERP.Domain.Events;
using MyERP.Domain.Shared.Users;
using MyERP.Events.Bus.Entities;

namespace MyErp6.Domain.Shared.Events.{{ feature_name_plural }}
{
    /// <summary>
    /// Domain event data raised when a {{ feature_lower }} is deleted.
    /// </summary>
    public sealed class {{ feature_name }}DeletedEventData<TEntity> : EntityDeletedEventData<TEntity>, IEntityDeletedEventData
    {
        /// <summary>
        /// Creates event data for a {{ feature_lower }} deleted event.
        /// </summary>
        public {{ feature_name }}DeletedEventData(string host, TEntity entity, CurrentUserDto user) : base(entity)
        {
            Host = host;
            Deletor = user;
        }

        /// <summary>
        /// Gets the user who caused the event.
        /// </summary>
        public CurrentUserDto Deletor { get; set; }

        /// <summary>
        /// Gets the host where the event was raised.
        /// </summary>
        public string Host { get;}
    }
}
