using MyErp6.Application.Contracts.{{ feature_name_plural }};
using MyErp6.Domain.Shared.TPosResult;
using TMT.MyErp6.{{ use_case }}.{{ feature_name_plural }}.{{ folder }};
using TmtCo.Mediator;

namespace TMT.MyErp6.{{ use_case }}.{{ feature_name_plural }}
{
    /// <summary>
    /// Handles {% if is_command %}commands{% else %}queries{% endif %} for {{ feature_name_plural }}.
    /// </summary>
    public sealed class {{ handler_name }}({{ service_interface_name }} {{ service_param_name }}) :
        {{ handler_interface }}
    {
        /// <summary>
        /// Handles {{ request_name }}.
        /// </summary>
        public async Task<{{ tpos_result_type }}> Handle({{ request_name }} request, CancellationToken cancellationToken)
            => await {{ service_param_name }}.{{ service_method_name }}({{ handler_service_call_args }});
    }
}
